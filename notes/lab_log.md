# CareerFit 실험 로그

기록 규칙: **버린 것부터 쓴다.** 성공한 프롬프트만 남은 로그는 포트폴리오에서 가치가 없다.
면접에서 팔리는 건 "왜 그걸 안 골랐는지"다.

형식:

## YYYY-MM-DD | 무엇을 시도했나

- 가설:
- 방법:
- 결과(숫자):
- 판정: 채택 / 폐기 / 보류
- 폐기 이유:

---

## 2026-07-24 | 수집 스키마 결정

- 가설: 파싱된 필드만 저장하면 추출 방식을 바꿀 때마다 재크롤링해야 한다.
- 결정: 원문(sections + body_raw) + API 응답 전체(api_raw)를 통째로 보존.
- 폐기한 대안: 기존 P3 크롤러처럼 tech_stack만 저장 → 4번 평가 자체가 불가능해서 폐기.
- 미해결: api_raw 용량. 일일 수집 30일 누적 시 재검토.

1024 수집 결과에 인사·총무 등 비데이터 직무 혼입 확인 — 정제 단계에서 필터 필요.

# CareerFit 실험 로그

기록 규칙: **버린 것부터 쓴다.** 성공한 프롬프트만 남은 로그는 포트폴리오에서 가치가 없다.
면접에서 팔리는 건 "왜 그걸 안 골랐는지"다.

---

## 2026-07-25 | 수집 스키마 결정

- 결정: 원문(sections + body_raw) + API 응답 전체(api_raw)를 통째로 보존.
- 근거: 추출을 앞으로 수십 번(regex/zero-shot/few-shot/캐스케이드) 다시 돌린다.
  파싱된 필드만 저장하면 방식 바꿀 때마다 재크롤링해야 하고, 마감 공고는 사라진다.
- 폐기한 대안: 기존 P3 크롤러처럼 tech_stack만 저장 → 4번 평가 자체가 불가능해서 폐기.

## 2026-07-25 | [실패] 목록 API 파라미터 오류

- 증상: --job-ids 를 1024/655/1634 뭘 줘도 매번 같은 60건(전 직군 최신순)만 수집됨.
  240건 합쳐 dedup하니 60건, 내용은 화장품 마케팅·인사·총무 등 비데이터 직무.
- 원인: /api/v4/jobs (구버전) 엔드포인트에 job_ids 파라미터를 넘겼으나 무시됨.
- 해결: 브라우저 DevTools Network 탭으로 실제 호출 URL을 리버싱.
  진짜 엔드포인트는 /api/chaos/navigation/v1/results 였고,
  job_group_id + job_ids 를 세트로 넘겨야 필터가 걸림. job_sort=job.latest_order 도 필수.
- 교훈: 비공개 API는 추측하지 말고 실제 트래픽을 캡처해서 확인한다.

## 2026-07-25 | [발견] 데이터 분석가 직군 분류

- 관찰: 데이터 사이언티스트·엔지니어·ML엔지니어는 '개발' 직군(job_group_id=518)이지만,
  데이터 분석가만 '경영·비즈니스' 직군(507)에 분류돼 있음.
- 영향: da 폴더(656) 수집 결과에 마케팅·영업·인사 공고가 대량 혼입됨.
- 판정: 버그 아님. 채용시장이 데이터 분석가를 엔지니어링이 아닌 비즈니스 직무로 본다는 증거.
  → 계획서 6번(직무별 역량 비교) / 10번(직무 클러스터링)의 분석 재료로 사용.
- 미결정: da 오염 처리 방식 (category_tags 필터링 vs 3직무로 축소). 정제 단계에서 결정.

## 2026-07-25 | [실패] jsonl 읽기 시 레코드 절단

- 증상: dump_for_reading 실행 시 JSONDecodeError (Unterminated string).
  그러나 줄 단위로 검사하면 240줄 전부 정상 JSON. 모순.
- 원인: load()가 read_text().splitlines() 사용. splitlines()는 \n 뿐 아니라
  유니코드 줄바꿈(\u2028 등)까지 잘라내서, 본문에 그 문자가 든 한 줄짜리 레코드를 두 동강 냄.
- 해결: 파일 객체를 직접 순회(for line in fh)하도록 교체.
- 교훈: jsonl 파싱에 splitlines() 금지. 데이터가 아니라 읽는 코드가 틀릴 수 있다.

## 2026-07-25 | [발견] 원티드 skill_tags는 정답셋으로 못 씀

- 관찰: 수집 150건의 원티드 skill_tags 상위가 AWS·Python 다음에
  Spring Framework(23)·React(23)·TypeScript(22)·Java(20) 등 범용 웹 스택.
  데이터 직무 공고인데도 프론트/백엔드 태그가 섞여 있고, 태그 '없음' 공고도 다수.
- 결론: 회사가 대충 단 태그라 신뢰 불가 → 계획서 4번(자체 정답셋 구축)의 존재 이유가 데이터로 증명됨.
  추출은 태그가 아니라 본문에서 해야 함.

## 2026-07-25 | 역량 태그 사전 v0 작성

- 방법: 수집 150건(ds/de/mle) 본문을 훑어 실제 등장한 표현만 수록. 상상 태그 없음.
- 규모: 8개 카테고리, canonical 72개, alias 219개.
- 필드 분포: ①명시적 스택 46 / ②서술형 역량 25 / ③암묵 조건 1.
  → ①은 regex 우세 예상, ②는 LLM 우세 예상 구간(4번 개선효과 측정 대상),
  ③은 태그화가 거의 불가능(v0 한계로 기록).
- 상태: v0. 라벨링하며 누락 태그·오탐 alias 나오면 v1으로 개정 예정.

# CareerFit 실험 로그

기록 규칙: **버린 것부터 쓴다.** 성공한 프롬프트만 남은 로그는 포트폴리오에서 가치가 없다.
면접에서 팔리는 건 "왜 그걸 안 골랐는지"다.

---

## 2026-07-25 | 수집 스키마 결정

- 결정: 원문(sections + body_raw) + API 응답 전체(api_raw)를 통째로 보존.
- 근거: 추출을 앞으로 수십 번(regex/zero-shot/few-shot/캐스케이드) 다시 돌린다.
  파싱된 필드만 저장하면 방식 바꿀 때마다 재크롤링해야 하고, 마감 공고는 사라진다.
- 폐기한 대안: 기존 P3 크롤러처럼 tech_stack만 저장 → 4번 평가 자체가 불가능해서 폐기.

## 2026-07-25 | [실패] 목록 API 파라미터 오류

- 증상: --job-ids 를 1024/655/1634 뭘 줘도 매번 같은 60건(전 직군 최신순)만 수집됨.
  240건 합쳐 dedup하니 60건, 내용은 화장품 마케팅·인사·총무 등 비데이터 직무.
- 원인: /api/v4/jobs (구버전) 엔드포인트에 job_ids 파라미터를 넘겼으나 무시됨.
- 해결: 브라우저 DevTools Network 탭으로 실제 호출 URL을 리버싱.
  진짜 엔드포인트는 /api/chaos/navigation/v1/results 였고,
  job_group_id + job_ids 를 세트로 넘겨야 필터가 걸림. job_sort=job.latest_order 도 필수.
- 교훈: 비공개 API는 추측하지 말고 실제 트래픽을 캡처해서 확인한다.

## 2026-07-25 | [발견] 데이터 분석가 직군 분류

- 관찰: 데이터 사이언티스트·엔지니어·ML엔지니어는 '개발' 직군(job_group_id=518)이지만,
  데이터 분석가만 '경영·비즈니스' 직군(507)에 분류돼 있음.
- 영향: da 폴더(656) 수집 결과에 마케팅·영업·인사 공고가 대량 혼입됨.
- 판정: 버그 아님. 채용시장이 데이터 분석가를 엔지니어링이 아닌 비즈니스 직무로 본다는 증거.
  → 계획서 6번(직무별 역량 비교) / 10번(직무 클러스터링)의 분석 재료로 사용.
- 미결정: da 오염 처리 방식 (category_tags 필터링 vs 3직무로 축소). 정제 단계에서 결정.

## 2026-07-25 | [실패] jsonl 읽기 시 레코드 절단

- 증상: dump_for_reading 실행 시 JSONDecodeError (Unterminated string).
  그러나 줄 단위로 검사하면 240줄 전부 정상 JSON. 모순.
- 원인: load()가 read_text().splitlines() 사용. splitlines()는 \n 뿐 아니라
  유니코드 줄바꿈(\u2028 등)까지 잘라내서, 본문에 그 문자가 든 한 줄짜리 레코드를 두 동강 냄.
- 해결: 파일 객체를 직접 순회(for line in fh)하도록 교체.
- 교훈: jsonl 파싱에 splitlines() 금지. 데이터가 아니라 읽는 코드가 틀릴 수 있다.

## 2026-07-25 | [발견] 원티드 skill_tags는 정답셋으로 못 씀

- 관찰: 수집 150건의 원티드 skill_tags 상위가 AWS·Python 다음에
  Spring Framework(23)·React(23)·TypeScript(22)·Java(20) 등 범용 웹 스택.
  데이터 직무 공고인데도 프론트/백엔드 태그가 섞여 있고, 태그 '없음' 공고도 다수.
- 결론: 회사가 대충 단 태그라 신뢰 불가 → 계획서 4번(자체 정답셋 구축)의 존재 이유가 데이터로 증명됨.
  추출은 태그가 아니라 본문에서 해야 함.

## 2026-07-25 | 역량 태그 사전 v0 작성

- 방법: 수집 150건(ds/de/mle) 본문을 훑어 실제 등장한 표현만 수록. 상상 태그 없음.
- 규모: 8개 카테고리, canonical 73개, alias 219개.
- 필드 분포: ①명시적 스택 46 / ②서술형 역량 25 / ③암묵 조건 1(협업커뮤니케이션).
  → ①은 regex 우세 예상, ②는 LLM 우세 예상 구간(4번 개선효과 측정 대상),
  ③은 태그화가 거의 불가능(v0 한계로 기록).
- 상태: v0. 라벨링하며 누락 태그·오탐 alias 나오면 v1으로 개정 예정.

## 2026-07-25 | [결정] 스킬 과제 범위 축소 (regex 추출 + 빈도표)

- 원래 처리 5단계 중 3단계(LLM 추출)·4단계(regex vs LLM precision/recall/F1)를 오늘 범위에서 뺌.
- 근거 1: LLM은 비결정적이라 "같은 입력=같은 출력" 재현 검증이 불가능 → 오늘 스킬 성격(재현 가능한 리포트)에 안 맞음.
- 근거 2: 성능 평가·캐스케이드는 계획서가 정한 프로젝트 본체(4·5번). 스킬 과제 하루에 욱여넣어 대충 할 게 아니라 4회차로 온전히 이관.
- 오늘 범위 확정: 1단계 로드·검증 → 2단계 regex 베이스라인 추출 → 3단계 직무별 빈도표 집계.

## 2026-07-25 | [결정] field 3(암묵조건) regex 집계 제외

- field 3 태그는 1개(협업커뮤니케이션)뿐이고 문맥 의존이라 regex(alias 매칭)로 잡을 수 없음.
- 필드 난이도별 집계표는 ①명시 / ②서술 2종류만 산출. field 3은 "집계 제외 태그"로 분류하고 개수·목록만 로그에 남김.
- ③ 암묵조건의 추출·평가는 4회차 LLM 추출로 이관 (계획서 4번: 개선 효과를 ②③에서 측정).
- field 분포 ①46 ②25 ③1의 극단적 쏠림 자체가 "암묵조건은 regex의 영역이 아니다"라는 발견.

## 2026-07-25 | [결정] 추출 대상 텍스트 = body_raw 전체 (섹션 사전배제 안 함)

- 태그의 field(1/2/3)와 공고의 section(main_tasks 등)은 별개 축임을 명시.
  field = 태그 사전에 정의된 태그 고유 속성, section = 텍스트상 발견 위치.
- regex는 body_raw 전체를 스캔(intro·benefits 포함). 섹션을 노이즈로 사전 배제하지 않음
  — 그 판단은 LLM 추출(4회차)의 몫. 실제로 intro에 역량 표현이 든 공고 존재(예: 공고 [28]).
- sections는 "발견된 태그가 어느 섹션에서 나왔나"(matched_fields 컬럼) 표시용으로만 사용.

## 2026-07-25 | [결정] 검증 요약표에 불변식 강제 + 직무불일치 컬럼화

- 문제: da 60건 중 통과 26 + 스킵 0 → 34건이 표 밖으로 증발. 제목에 데이터 직무 키워드 없는 건(영업기획·재무 등)이 "직무불일치"로 분리됐으나 Q5 확정 6컬럼에 자리가 없었음.
- 결정: validation*summary.csv에 '직무불일치' 컬럼 추가.
  불변식 강제: 입력공고수 = 통과 + 스킵*파싱실패 + 스킵*필드누락 + 스킵*본문결측 + 직무불일치.
  실행 시 이 등식이 안 맞으면 에러 중단.
- da 직무불일치 34/60건. 원티드 직군 분류 문제(507)가 수치로 드러남 → 계획서 6번 재료.
- 분리 건은 삭제 아님. \_job_mismatch_da.txt 로 보존.

## 2026-07-25 | [검증] 재현성 통과

- 같은 입력(240건)으로 스킬 2회 실행 → 4개 공식 출력 + 2개 진단 로그 전부 바이트 단위 동일(diff 무출력).
- 성립 이유: regex 이진 매칭(비결정 요소 없음) + set 알파벳순 정렬 + 동점 canonical 정렬 + 소수점 2자리 고정 + 불변식 검사.
- 결론: 오늘 범위를 regex로 좁힌 결정이 재현성으로 값어치 함. LLM이었으면 이 검증 자체가 불가능.

---

## 다음 세션 진입점

1. 라벨링 포맷 설계 (4번 정답셋의 출발점). 태그 사전 v0 확정됐으므로 착수 가능.
2. da 오염 최종 처리 결정 (category_tags 필터링 vs da 접고 3직무 축소). 34건은 이미 분리 보존됨.

## 2026-08-01 | regex 추출 run() MCP 대비 리팩터

- run()에 입력 경로 인자화(raw_dir/dict_path/out_dir, 기본값=기존 경로) + 처리 요약 dict 반환 추가. 로직 불변, 실데이터 재실행 시 CSV 4종 동일(통과 206/직무불일치 34/진단 11).
- 요약 top_tags에서 field 3(협업커뮤니케이션) 제외해 CSV ④와 기준 일치. (제외 전 협업이 2위로 잡혀 오해 소지)
- 상위 10 역량: Python, LLM, AI Agent, 파이프라인설계운영, SQL, 데이터품질관리, AWS, MLOps, PyTorch, 모델서빙배포 → 목표 직무(데이터 기반 AI 엔지니어)와 일치. 6번 시장분석 미리보기.
- 다음(8/1 수업): 이 run()을 MCP 도구로 감싼다.
